name: Update Badges

on:
  schedule:
    #Runs at 0am UTC every day
    - cron: "0 0 * * * *"
jobs:
  update-readme:
    name: Update Readme with Badges
    runs-on: ubuntu-latest
    permissions:
      contents: write
      steps:
        - name: Badges - Readme
          uses: pat-glitch/badge-readme@main